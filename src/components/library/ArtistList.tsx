// Cadence — Artist list component
// Displays artists with their album/song counts

export default function ArtistList() {
  // TODO: Implement artist list with avatars, names, and stats
  return (
    <div id="artist-list" className="artist-list">
      {/* Artist items */}
    </div>
  );
}
