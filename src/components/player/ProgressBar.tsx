// Cadence — Playback progress bar component
// Shows current position, allows seeking, and displays elapsed/remaining time

export default function ProgressBar() {
  // TODO: Implement seekable progress bar with time display
  return (
    <div id="progress-bar" className="progress-bar">
      {/* Elapsed time */}
      {/* Seekable slider */}
      {/* Total/remaining time */}
    </div>
  );
}
